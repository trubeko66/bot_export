# Руководство по авторизации Telegram Bot / Telegram Bot Authorization Guide

## 📋 Содержание

- [🚀 Быстрый старт](#-быстрый-старт)
- [⚙️ Настройка окружения](#️-настройка-окружения)
- [🔑 Получение учетных данных](#-получение-учетных-данных)
- [🐳 Docker авторизация](#-docker-авторизация)
- [💻 Локальная авторизация](#-локальная-авторизация)
- [🧪 Тестирование](#-тестирование)
- [🚨 Решение проблем](#-решение-проблем)
- [📞 Поддержка](#-поддержка)

---

## 🚀 Быстрый старт

### 1. Настройка
```bash
# Скопируйте шаблон настроек
copy .env.template .env

# Заполните в .env:
BOT_TOKEN=ваш_токен_бота
API_ID=ваш_api_id
API_HASH=ваш_api_hash
PHONE_NUMBER=+79123456789  # ОБЯЗАТЕЛЬНО с + и кодом страны!
CLOUD_PASSWORD=ваш_пароль_2fa  # если включена 2FA
```

### 2. Авторизация

**Windows (обновлено для docker compose):**
```cmd
docker-auth-new.bat
```

**Linux/macOS (обновлено для docker compose):**
```bash
chmod +x docker-auth-new.sh
./docker-auth-new.sh
```

**Интерактивное тестирование (при проблемах с кодом):**
```bash
# Диагностика и интерактивная авторизация
py interactive_auth_test.py
```

**Ручная авторизация:**
```bash
# 1. Запустите контейнер
docker compose up -d

# 2. Получите код из Telegram и выполните:
docker compose exec -e TELEGRAM_CODE=ваш_код telegram-bot python auto_auth.py
```

### 3. Запуск бота
```bash
docker compose up -d
```

---

## ⚙️ Настройка окружения

### Переменные окружения

Обязательные переменные в файле `.env`:

| Переменная | Описание | Пример | Обязательно |
|------------|----------|--------|-------------|
| `BOT_TOKEN` | Токен бота от @BotFather | `123456789:ABC...` | ✅ |
| `API_ID` | API ID от my.telegram.org | `12345678` | ✅ |
| `API_HASH` | API Hash от my.telegram.org | `abcdef123...` | ✅ |
| `PHONE_NUMBER` | Номер телефона (международный формат) | `+79123456789` | 🐳 Docker |
| `CLOUD_PASSWORD` | Пароль 2FA (если включен) | `mypassword` | ❌ |
| `TELEGRAM_CODE` | Код подтверждения из Telegram | `12345` | ⚡ Временно |
| `CODE_WAIT_TIMEOUT` | Таймаут ожидания кода (сек) | `60` | ❌ |

---

## 🔑 Получение учетных данных

### Токен бота (@BotFather)

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Выберите имя для бота
   - Выберите username (должен заканчиваться на 'bot')
4. Скопируйте предоставленный токен

### API учетные данные (my.telegram.org)

1. Перейдите на [my.telegram.org](https://my.telegram.org)
2. Войдите с номером телефона
3. Выберите "API Development Tools"
4. Создайте новое приложение:
   - **App title**: "Channel Export Bot"
   - **Short name**: "export_bot"
   - **Platform**: Other
5. Скопируйте `api_id` и `api_hash`

---

## 🐳 Docker авторизация

### Автоматизированные скрипты (Рекомендуется)

**Windows:**
```cmd
docker-auth-new.bat
```

**Linux/macOS:**
```bash
chmod +x docker-auth-new.sh
./docker-auth-new.sh
```

### Ручная авторизация Docker

```bash
# 1. Запустите контейнер
docker compose up -d

# 2. Получите код подтверждения из Telegram
# 3. Выполните авторизацию с кодом:
docker compose exec -e TELEGRAM_CODE=12345 telegram-bot python auto_auth.py

# 4. Если требуется 2FA:
docker compose exec -e CLOUD_PASSWORD=ваш_пароль telegram-bot python auto_auth.py
```

### Интерактивная авторизация Docker

```bash
# Подключение для интерактивной авторизации
docker compose exec telegram-bot python interactive_auth_test.py
```

---

## 💻 Локальная авторизация

### Автоматическая авторизация

```bash
# Настройте переменные окружения
export PHONE_NUMBER="+79123456789"
export TELEGRAM_CODE="12345"  # Получите из Telegram
export CLOUD_PASSWORD="пароль"  # Если включена 2FA

# Запустите авторизацию
python auto_auth.py
```

### Интерактивная авторизация

```bash
# Интерактивное тестирование и авторизация
python interactive_auth_test.py
```

---

## 🧪 Тестирование

### Тесты авторизации

```bash
# Проверка системы авторизации
python test_auth.py

# Интерактивная диагностика
python interactive_auth_test.py

# Тест автоматической авторизации
python auto_auth.py
```

### Тесты экспорта

```bash
# Тестирование форматов экспорта
python test_export_formats.py
python test_simple.py
python test_markdown.py
```

---

## 🚨 Решение проблем

### Частые ошибки и решения

#### "Код подтверждения не приходит"

**Причины и решения:**

1. **Неправильный формат номера телефона**
   - ✅ Правильно: `+79123456789`
   - ❌ Неправильно: `79123456789`, `8-912-345-67-89`

2. **Неверные API данные**
   - Получите новые учетные данные на [my.telegram.org](https://my.telegram.org)
   - Убедитесь, что API_ID и API_HASH правильно скопированы

3. **Проблемы с переменными окружения**
   - Проверьте файл `.env` на наличие лишних пробелов
   - Убедитесь, что нет кавычек вокруг значений

#### "API ID or Hash cannot be empty"

```bash
# Проверьте содержимое .env файла
cat .env  # Linux/macOS
type .env  # Windows

# Убедитесь, что файл содержит:
API_ID=12345678
API_HASH=abcdef1234567890
```

#### "Invalid phone number"

- Номер должен начинаться с `+` и кода страны
- Для России: `+7`, для Украины: `+380`, для США: `+1`
- Не используйте пробелы, дефисы или скобки

#### "Phone code expired"

- Код действителен только 5 минут
- Запросите новый код и введите быстрее
- Используйте интерактивную авторизацию для большего контроля

#### "Phone code invalid"

- Вводите только цифры кода без пробелов
- Убедитесь, что код приходит на правильный номер
- Проверьте, что номер в `.env` совпадает с номером в Telegram

#### "Flood wait"

- Слишком много попыток авторизации подряд
- Подождите указанное время перед новой попыткой
- Используйте `interactive_auth_test.py` для диагностики

### Диагностические команды

```bash
# Проверка статуса Docker
docker compose ps

# Просмотр логов контейнера
docker compose logs telegram-bot

# Проверка переменных окружения в контейнере
docker compose exec telegram-bot env | grep -E "API_|BOT_|PHONE"

# Остановка контейнера
docker compose down

# Удаление старой сессии для повторной авторизации
del bot_session.session  # Windows
rm bot_session.session   # Linux/macOS
```

### Управление Docker

```bash
# Статус сервисов
docker compose ps

# Логи в реальном времени
docker compose logs -f

# Остановка всех сервисов
docker compose down

# Перезапуск бота
docker compose restart telegram-bot

# Полная пересборка
docker compose up -d --build
```

---

## 📞 Поддержка

### Самостоятельная диагностика

1. **Проверьте переменные окружения** в файле `.env`
2. **Убедитесь в правильности формата номера** телефона
3. **Попробуйте интерактивную авторизацию** `interactive_auth_test.py`
4. **Проверьте логи** на наличие подробной информации об ошибках

### Контрольный список

- [ ] Файл `.env` существует и правильно заполнен
- [ ] Номер телефона в международном формате с `+`
- [ ] API_ID и API_HASH получены с [my.telegram.org](https://my.telegram.org)
- [ ] BOT_TOKEN получен от [@BotFather](https://t.me/BotFather)
- [ ] Код подтверждения приходит на указанный номер
- [ ] Docker контейнер запущен (для Docker авторизации)

### Полезные файлы

- `auth_helper.py` - автоматическая авторизация (обновлено)
- `auto_auth.py` - тестовая авторизация  
- `docker-auth-new.sh/bat` - новые скрипты для docker compose
- `interactive_auth_test.py` - интерактивная диагностика
- `test_auth.py` - тестирование системы

---

**⚠️ ВАЖНО: Используйте команду `docker compose` (современная) вместо `docker-compose` (устаревшая)**

**💡 Совет**: Код подтверждения должен приходить на номер телефона, указанный в переменной `PHONE_NUMBER` в файле `.env`.